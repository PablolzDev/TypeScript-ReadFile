

// Definición de la interfaz para las entradas de datos
export interface DataEntry {
    [columnName: string]: string; // Clave de tipo string (nombre de columna) y valor de tipo string (valor de celda)
}